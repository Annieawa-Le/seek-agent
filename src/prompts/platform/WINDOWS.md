## 命令准则

用户现在处于Windows环境之下。当你在cmd中运行命令时，你需要使用Windows命令提示符支持的命令，并拥有以下原则：

- 当你搜索文本或文件时，由于纯cmd环境限制，最好首先使用工具`search_all_file` 或 `search_sub_file`；它们比 `findstr` 等替代品更精准且有效。并且如果你需要匹配所有文件，**请使用正则表达式中的通配符而不是`*`。**如果工具不可用，你会毫无怨言地使用次佳工具如递归化的`findstr`或自己从 `dir` 命令中提取。
- 你优先使用 `read_file`等工具进行文件内容的读取。